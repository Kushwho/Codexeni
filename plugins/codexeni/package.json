{
  "name": "codexeni",
  "version": "0.1.0-dev.20260905192415",
  "private": true,
  "type": "module",
  "description": "Codexeni MCP bridge that lets one coding harness delegate bounded tasks to another local coding harness.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --outfile=dist/index.js --sourcemap",
    "bump": "node scripts/bump-version.mjs",
    "check:cycles": "madge --circular --extensions ts src/",
    "check:manifests": "node scripts/bump-version.mjs --check",
    "inspect": "pnpm build && npx @modelcontextprotocol/inspector --config inspector.json --server codexeni",
    "inspect:cli": "pnpm build && npx @modelcontextprotocol/inspector --cli --config inspector.json --server codexeni",
    "lint": "eslint src",
    "start": "node dist/index.js",
    "test": "pnpm build && node --test --test-concurrency=1 test/runtime.test.mjs test/interactive.test.mjs test/claude-code.test.mjs test/codex.test.mjs test/adapters.test.mjs test/bundle.test.mjs",
    "test:fixture": "node --test fixtures/clamp/test/clamp.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.9",
    "eslint": "^10.10.0",
    "eslint-plugin-import": "^2.32.0",
    "madge": "^8.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.70.0"
  }
}
